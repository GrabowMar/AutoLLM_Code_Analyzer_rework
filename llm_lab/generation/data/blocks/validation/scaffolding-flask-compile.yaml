slug: scaffolding-flask-compile
version: 1
block_type: validation
name: Flask compile check
description: Post-generation Python syntax validation for Flask scaffolds
metadata:
  check: py_compile
  target: app.py
content: |
  Validate generated backend with `python -m py_compile app.py` before marking the job complete.
